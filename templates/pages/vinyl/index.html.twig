{% extends 'base.html.twig' %}

{% block title %}Vinyl
{% endblock %}

{% block body %}

    {# TODO: add an image of the record #}

    <div>
        Tracks :

        <ul>
            {% for track in tracks %}
                <li>
                    {{ track.song }} - {{ track.artist }}
                </li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
